function opponentNoteHit(id, direction, noteType, isSustainNote)
    if getHealth() > 0.05 then
        if getProperty('dad.animation.curAnim.name') ~= 'idle' and dadName == 'FNF/corrupted-bf' then
        addHealth(-0.02)
        end
    end
end